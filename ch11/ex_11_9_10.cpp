#include <iostream>
#include <map>
#include <vector>
#include <list>
using std::string;
using std::map;
using std::vector;
using std::cout;
using std::endl;
using std::list;

int main()
{
	map<string, list<int>> mp;
	map<vector<int>::iterator, int> mv;
	map<list<int>::iterator, int> ml;
	return 0;
}